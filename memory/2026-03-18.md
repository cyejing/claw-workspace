# 2026-03-18 工作记录

## 工具使用问题

### web_search 工具限制
使用 Gemini provider 时，以下参数**不支持**：
- `language` - 语言过滤不支持，只有 Brave 和 Perplexity 支持
- `freshness` - 时间过滤不支持，只有 Brave 和 Perplexity 支持

**正确用法：** 使用 Gemini 时只传 `query` 和 `count` 参数，不传 language/freshness。

---
