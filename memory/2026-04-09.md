# 2026-04-09 工作日志

## 系统监控脚本修复

- 问题：system-monitor 脚本执行后被 SIGKILL 杀死
- 原因：exec session 处理 stdout 有问题，脚本内 sleep 会导致 session 卡住
- 解决：脚本末尾使用 `os._exit(0)` 强制退出，通过 background 模式运行避免 stdout 问题
- 修改：
  - collect_stats.py 末尾添加 `os._exit(0)`
  - SKILL.md 更新执行说明
  - 改为单次采样（N=1）减少耗时